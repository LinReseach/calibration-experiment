Here is the code about how to analyze the processed data, before that we take all the useful information into one DataFrame. Including the participants(ID number, eye height, glasses wearing or not), screen dots places, human positions, and the computed data(3D error, 2D error, estimated 2d on the virtual screen). Then we did a lot of analysis on the dataset(data frame)
